<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
		</layout>
	</appender>

	<appender name="FILE" class="ch.qos.logback.core.FileAppender">
		<file>testFile.log</file>
		<append>true</append>
		<!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder 
			by default -->
		<encoder>
			<pattern>%-4relative [%thread] %-5level %logger{35} - %msg</pattern>
		</encoder>
	</appender>

	<!-- STDOUT is defined above but deliberately unused by default: Lanterna owns the terminal's
		cursor positioning while the app is running, and any concurrent console output (this logger
		included) visually corrupts its screen. Swap FILE for STDOUT below only when running outside
		the terminal UI (e.g. non-interactively). -->
	<logger name="com.jasonwjones" level="info" additivity="false">
		<appender-ref ref="FILE" />
	</logger>
	<root level="error">
		<appender-ref ref="FILE" />
	</root>
</configuration>